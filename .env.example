# Optional: Set your Anthropic API key for AI-powered article summaries
# Without this, the app works in basic mode using RSS descriptions
ANTHROPIC_API_KEY=your_key_here

# Optional: Protect mutation API routes (fetch, summarize) with a secret token
# If not set, all requests are allowed (fine for local/personal use)
# If set, POST requests require: Authorization: Bearer <token> or ?token=<token>
API_SECRET=
